    ```yml
    version: "3.9"

    services:
      db:
        image: postgres:15
        restart: always
        env_file:
          - .env
        volumes:
          - postgres_data:/var/lib/postgresql/data

      app:
        image: a18091986edu/fastapi-app:latest
        depends_on:
          - db
        ports:
          - "80:8000"
        env_file:
          - .env

    volumes:
      postgres_data:
    ```