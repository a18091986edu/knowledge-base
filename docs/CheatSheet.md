## linux
hostnamectl set-hostname # задать имя хоста
less file - интерактивное чтение файла

passwd - сменить пароль
useradd -m -s /bin/bash username # создать пользователя с домашней директорией и указать оболочку
usermod -aG sudo username # добавить пользователя в группу sudo a - append (чтобы сохранить остальные группы пользователя), G - groups (список дополнительных групп)
userdel behappyman
